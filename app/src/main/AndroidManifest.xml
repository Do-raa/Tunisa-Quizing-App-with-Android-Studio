<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools">

<application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/logo_tunisia"
    android:label="Tunisia"
    android:roundIcon="@mipmap/logo_tunisia"
    android:supportsRtl="true"
    android:theme="@style/Theme.QuizingApp"
    tools:targetApi="31">

    <!-- Launcher Activity -->
    <activity
        android:name=".LoginActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <!-- Other Activities -->
    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.QuizingApp.NoActionBar">
    </activity>
    <activity
        android:name=".SightseeingActivity"
        android:exported="false">
    </activity>
    <activity
        android:name=".ReligionActivity"
        android:exported="false">
    </activity>
    <activity
        android:name=".FoodActivity"
        android:exported="false">
    </activity>
    <activity
        android:name=".MusicActivity"
        android:exported="false">
    </activity>

</application>

</manifest>
